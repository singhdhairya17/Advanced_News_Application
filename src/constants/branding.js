/** App display name — navbar, footer (change once, updates everywhere in the UI). */
export const APP_NAME = 'Citizen Post'

/** Short line under the wordmark in the header. */
export const APP_TAGLINE = 'Independent headlines'
